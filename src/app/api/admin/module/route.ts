import { NextRequest } from 'next/server';
import { adminDb } from '@/lib/firebase-admin';
import { requireRole } from '@/lib/server-auth';
import { fail, ok } from '@/lib/api-response';
export const dynamic='force-dynamic'; export const revalidate=0;
const map:Record<string,string[]>={tournaments:['tournaments'],users:['users'],tasks:['tasks','taskClaims'],notifications:['notifications'],security:['securityEvents'],logs:['auditLogs'],system:['systemConfig'],database:['users','tournaments','coinLedger'],health:['users','tournaments'],moderation:['reports'],reports:['reports']};
export async function GET(request:NextRequest){try{await requireRole(request,['ADMIN','SUPER_ADMIN','EDITOR','MODERATOR']);if(!adminDb)return fail('Admin database is not configured',503);const module=String(request.nextUrl.searchParams.get('module')||'').trim().toLowerCase();const collections=map[module];if(!collections)return fail('Unknown admin module',400);const data=await Promise.all(collections.map(async collection=>{try{const snap=await adminDb!.collection(collection).limit(50).get();return{collection,count:snap.size,items:snap.docs.map(doc=>({id:doc.id,...doc.data()})).slice(0,20)}}catch(error){console.error(`Admin module collection read failed: ${collection}`,error);return{collection,count:0,items:[],error:'Collection unavailable'}}}));return ok({module,collections:data,generatedAt:Date.now()})}catch(error:any){if(error?.message==='UNAUTHORIZED')return fail('Authentication required',401);if(error?.message==='FORBIDDEN')return fail('Forbidden',403);return fail('Unable to load admin module',500)}}
