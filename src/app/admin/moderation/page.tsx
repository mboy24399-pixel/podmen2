import AdminModulePage from "@/components/admin/AdminModulePage";
export default function Page(){return <AdminModulePage title="Content Moderation" description="Review, approve, reject and restore platform content without exposing moderation controls to users." features={["Moderation queue","Publish / reject workflow","Soft delete & restore","Bulk moderation"]}/>}
