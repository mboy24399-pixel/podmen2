"use client";
import { useEffect,useState } from 'react';
import { RefreshCw,ShieldAlert,UserRound,Ban,Pencil,Check,X } from 'lucide-react';
import { useAuth } from '@/context/AuthContext';

export default function AdminUsers(){
 const{ idToken }=useAuth();const[rows,setRows]=useState<any[]>([]);const[loading,setLoading]=useState(false);const[error,setError]=useState('');const[editing,setEditing]=useState<string|null>(null);const[draft,setDraft]=useState({displayName:'',role:'USER',email:''});const[busy,setBusy]=useState<string|null>(null);
 const load=async()=>{if(!idToken)return;setLoading(true);setError('');try{const r=await fetch('/api/admin/users?limit=100',{headers:{Authorization:`Bearer ${idToken}`},cache:'no-store'});const j=await r.json();if(!r.ok)throw new Error(j.error||'Unable to load users');setRows(j.data.items||[])}catch(e:any){setError(e.message||'Unable to load users')}finally{setLoading(false)}};
 useEffect(()=>{load()},[idToken]);
 const mutate=async(payload:any)=>{if(!idToken)return;setBusy(payload.userId+payload.action);try{const r=await fetch('/api/admin/users',{method:'PATCH',headers:{Authorization:`Bearer ${idToken}`,'Content-Type':'application/json'},body:JSON.stringify(payload)});const j=await r.json();if(!r.ok)throw new Error(j.error||'Update failed');setEditing(null);await load()}catch(e:any){setError(e.message||'Update failed')}finally{setBusy(null)}};
 const startEdit=(row:any)=>{setEditing(row.id);setDraft({displayName:row.displayName||'',role:row.role||'USER',email:row.email||''})};
 return <main className="space-y-6"><header className="skeuo-panel p-6"><p className="eyebrow">USER OPERATIONS</p><div className="mt-2 flex items-center justify-between gap-4"><div><h1 className="text-3xl font-black">Users</h1><p className="mt-1 text-sm text-dark-muted">Live accounts, roles, subscription state and access controls.</p></div><button onClick={load} className="skeuo-button inline-flex items-center gap-2"><RefreshCw size={16}/> Refresh</button></div></header>{error&&<div className="skeuo-card flex gap-3 p-4 text-sm text-red-300"><ShieldAlert size={18}/>{error}</div>}<div className="skeuo-panel overflow-x-auto"><table className="w-full min-w-[900px] text-left text-sm"><thead className="border-b border-dark-border text-xs uppercase text-dark-muted"><tr><th className="p-4">User</th><th className="p-4">Role</th><th className="p-4">Subscription</th><th className="p-4">Status</th><th className="p-4">Joined</th><th className="p-4 text-right">Actions</th></tr></thead><tbody>{rows.map(x=><><tr key={x.id} className="border-b border-dark-border/60 align-top"><td className="p-4"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-dark-surface text-accent shadow-skeuo-inset"><UserRound size={17}/></div><div className="min-w-0"><b className="block truncate">{x.displayName||x.email||x.id}</b><span className="text-xs text-dark-muted">{x.email||x.id}</span></div></div></td><td className="p-4 font-bold">{x.role||'USER'}</td><td className="p-4">{x.subscriptionStatus||(x.isSubscribed?'ACTIVE':'FREE')}</td><td className="p-4"><span className={`rounded-full px-2 py-1 text-[10px] font-black ${x.banned||x.suspended?'bg-red-500/10 text-red-300':'bg-emerald-500/10 text-emerald-300'}`}>{x.banned?'BLOCKED':x.suspended?'SUSPENDED':'ACTIVE'}</span></td><td className="p-4 text-dark-muted">{x.createdAt?new Date(x.createdAt).toLocaleDateString():'—'}</td><td className="p-4"><div className="flex justify-end gap-2"><button onClick={()=>startEdit(x)} className="skeuo-button inline-flex items-center gap-2 px-3 py-2 text-xs"><Pencil size={14}/> Edit</button><button disabled={!!busy} onClick={()=>mutate({userId:x.id,action:x.banned?'unblock':'block'})} className="skeuo-button inline-flex items-center gap-2 px-3 py-2 text-xs disabled:opacity-50"><Ban size={14}/>{x.banned?'Unblock':'Block'}</button></div></td></tr>{editing===x.id&&<tr><td colSpan={6} className="p-4 pt-0"><div className="skeuo-card grid gap-3 p-4 md:grid-cols-3"><input className="input" value={draft.displayName} onChange={e=>setDraft(v=>({...v,displayName:e.target.value}))} placeholder="Display name"/><input className="input" value={draft.email} onChange={e=>setDraft(v=>({...v,email:e.target.value}))} placeholder="Email" type="email"/><select className="input" value={draft.role} onChange={e=>setDraft(v=>({...v,role:e.target.value}))}>{['USER','PREMIUM_USER','EDITOR','MODERATOR','ADMIN','SUPER_ADMIN'].map(r=><option key={r}>{r}</option>)}</select><div className="flex gap-2 md:col-span-3"><button onClick={()=>mutate({userId:x.id,action:'edit',...draft})} className="skeuo-button-primary inline-flex items-center gap-2"><Check size={15}/> Save changes</button><button onClick={()=>setEditing(null)} className="skeuo-button inline-flex items-center gap-2"><X size={15}/> Cancel</button></div></div></td></tr>}</>)}{!loading&&!rows.length&&<tr><td colSpan={6} className="p-10 text-center text-dark-muted">No users found.</td></tr>}</tbody></table></div></main>
}
