rules_version = '2';

// TEST ONLY. Never deploy this file to production.
// Use the real firestore.rules file for production security.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
